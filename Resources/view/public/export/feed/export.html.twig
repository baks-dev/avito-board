<?xml version="1.0" encoding='UTF-8'?>

{# <Ads formatVersion='3' target='Avito.ru'> #}
{#    {% for product in products|filter(v => v.product_price > 0) %} #}
{#        <Ad> #}
{#            <Id> #}
{#                test #}
{#            </Id> #}
{#        </Ad> #}
{#    {% endfor %} #}
{# </Ads> #}

<Ads formatVersion='3' target='Avito.ru'>
    {% for product in products|filter(v => v.product_price > 0) %}

        {% set tags = element_transform(product) %}

        {% if tags is not null %}
            <Ad>
            {% for tag in tags %}
{#                {% autoescape false %}#}
{#                {{ tag }}#}
{#                {% endautoescape  %}#}

                <{{ tag.element }}>
{#                {{ tag.value|trans({}, 'avito-board.mapper') }}#}


                                {% autoescape false %}

{#                {% if tag.element == 'Description' %}#}
{#                    <![CDATA[#}
{#                    {{ tag.value }}#}
{#                    ]]>#}
{#                {% else %}#}
{#                    {{ tag.value }}#}
{#                {% endif %}#}

                                    {{ element_format(tag.element, tag.value) }}

                                {% endautoescape  %}



                </{{ tag.element }}>
            {% endfor %}
            </Ad>
        {% endif %}

    {% endfor %}
</Ads>
