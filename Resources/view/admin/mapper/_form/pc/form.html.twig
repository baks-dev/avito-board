{% trans_default_domain 'avito-board.admin' %}

<div>

    {% for setting in form.mapperSetting %}
        <div class="d-flex p-3 mb-1 gap-2 align-items-end rounded-3 border"
             style='background-color: {{ setting.vars.data.elementInstance.isMapping ?:'#fff2f2' }}'
        >
            {% if setting.productField is defined %}

                <div class="w-50">

                    {{ form_label(setting.productField) }}
                    {{ form_help(setting.productField) }}

                    {% if setting.vars.data.elementInstance.isMapping == false %}
                        текст предупреждения!!!
                    {% endif %}
                </div>

                <div class="w-50 d-flex mb-1 gap-2 align-items-end rounded-3">
                    {{ form_widget(setting.productField, {
                        placeholder: 'Выберите свойство продукта...',
                        attr: { 'data-select' : 'select2', class:  'mb-0'}
                    }) }}
                </div>

            {% endif %}

            {% if setting.def is defined %}

                <div class="w-50">
                    {{ form_label(setting.def) }}


                    {% if setting.def.vars.help is not null %}
                        <div>
                            {{ include (Template(setting.def.vars.help)) }}
                        </div>
                    {% endif %}

                    {% if setting.vars.data.elementInstance.isMapping == false %}
                        текст предупреждения!!!
                    {% endif %}

                </div>

                <div class="w-50 d-flex mb-1 gap-2 align-items-end rounded-3">
                    {{ form_widget(setting.def, {
                        placeholder: 'Значение по умолчанию...',
                        attr: { 'placeholder' : 'Значение по умолчанию'}
                    }) }}
                </div>

            {% endif %}
        </div>
    {% endfor %}

</div>

